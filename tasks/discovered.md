# Discovered Work — tax-billing

> Index: [[TASKS]]
> **Disposition key:** `open` | `promoted → [[tasks/milestone-NN-slug]] TASK-NNN` | `resolved (how/when)` | `rejected (reason)`
> **Severity:** leading `**[blocking]**` / `**[next]**` / `**[someday]**` in the Description cell (rows predating the convention carry none).

| ID | Description | Discovered during | Disposition |
|----|-------------|-------------------|-------------|
| DW-001 | `_get_db_params` fragile URL parsing in `backend/app/services/backup_service.py` — hand-splits `DATABASE_URL` and crashes on passwords containing `:`, `@`, or `/`. Latent (token_urlsafe passwords are URL-safe) but TASK-003's new generated password could trip it. Fix: use `urllib.parse.urlparse`. Proposed home: Milestone 6 (or a hardening task if TASK-003 surfaces a parse crash). | [[tasks/milestone-01-stop-the-bleeding]] TASK-001 | `open` |
| DW-002 | Backup file disk-leak on partial failure — if `pg_dump` succeeds and the dump file is written but the `BackupLog` insert/flush fails, the `.sql` file stays on disk with no log row. Soft leak. Fix: wrap in try/except and `unlink()` the file on log-insert failure. Proposed home: Milestone 6 hardening. | [[tasks/milestone-01-stop-the-bleeding]] TASK-001 | `open` |
| DW-003 | Line-ending drift — repo has mixed CRLF/LF; TASK-002 edits flipped `docker-compose.yml` and `.env.example` CRLF→LF, inflating diffs. Add `.gitattributes` with `* text=auto eol=lf` to normalize. Small, cosmetic. Proposed home: Milestone 6 or a one-off chore before Milestone 2. | [[tasks/milestone-01-stop-the-bleeding]] TASK-002 | `open` |
| DW-004 | Unused `from typing import Optional` in `backend/app/config.py` — will be caught by ruff in Milestone 2 (TASK-008); no separate action needed now. | [[tasks/milestone-01-stop-the-bleeding]] TASK-002 | `open` (resolved by [[tasks/milestone-02-quality-gates]] TASK-008) |
| DW-005 | Redundant `env_file: .env` on the `db` compose service — the `environment:` block already declares the `POSTGRES_*` vars via `${...}` substitution, so `env_file:` is redundant and also injects unrelated vars (JWT_SECRET_KEY, API_URL) into the postgres container. Not a security hole, just broader than necessary. Drop when revisiting compose (TASK-007 or later). | [[tasks/milestone-01-stop-the-bleeding]] TASK-002 | `open` |
| DW-006 | Stale CLAUDE.md gotcha re: auto-backup crash path — § Gotchas still says "Auto-backup code path is currently broken," but TASK-001 fixed that path in session 003 (PR #4). Misleading for future agents/humans. One-line deletion. Proposed home: next docs/chore window or bundled with another M1 PR. | [[tasks/milestone-01-stop-the-bleeding]] TASK-013 | `resolved (stale gotcha deleted in TASK-018 bookkeeping, 2026-09-01)` |
| DW-007 | Slug collision across clients sharing a first word — `_slug_client_name` uses the first whitespace-delimited word of `clients.name`, so e.g. "Adamson Systems" and "Adamson Foundation" would collide on their first invoice of the year. The `UNIQUE` constraint on `invoice_number` surfaces it as a loud 500 rather than silent overwrite. Fixes need DDL (explicit `short_name` column) — wait for M4 Alembic, or detect-and-disambiguate. Not urgent; current roster has no collision. | [[tasks/milestone-01-stop-the-bleeding]] TASK-015 | `open` |
| DW-008 | M2 unit-test coverage for `_slug_client_name` — when pytest infra lands in TASK-010, add tests for: empty string, whitespace-only, punctuation-only first word, non-ASCII-only first word, leading-whitespace input, case preservation, digit-containing slug ("BEE2 Corp"), happy path ("O'Reilly & Sons"). Fold into M2 TASK-011/012 decomposition. | [[tasks/milestone-01-stop-the-bleeding]] TASK-015 | `open` (target [[tasks/milestone-02-quality-gates]] TASK-011/012) |
| DW-009 | **[next]** `docker-compose.yml` has no top-level `name:`, and neither the `postgres_data` volume nor the `tax-billing-network` network is `name:`-pinned. Compose therefore derives the project name from the *directory basename*, so the real volume/network identities differ inside every `.claude/worktrees/` checkout — a worktree silently gets its own empty database instead of the canonical one ([[resource-naming]] § No Magic Names). Fix: add a top-level `name: tax-billing` plus explicit `name:` keys on the volume and network; a one-line-per-key compose change, but it renames the existing volume, so it needs a dump/restore or a deliberate re-init and must be user-gated (the volume holds real financial data). Proposed home: Milestone 5 (containerize frontend — compose is being touched anyway) or a standalone compose chore. | [[tasks/milestone-01-stop-the-bleeding]] TASK-018 | `open` |
